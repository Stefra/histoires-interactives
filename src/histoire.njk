---
pagination:
  data: histoires.ile_mystere
  size: 1
  alias: etape
permalink: "/histoires/ile-mystere/{{ etape.id }}/index.html"
# On dit à cette page d'utiliser le layout 'base.njk'
layout: base.njk
# On définit le titre pour ce layout, spécifique à l'étape
title: L'île aux Mystères - {{ etape.id }}
---

{# Plus besoin de <html>, <head>, <body> ici ! Ils sont dans base.njk #}
{# Le contenu ci-dessous sera inséré dans le {{ content | safe }} du layout #}

<h1>L'île aux Mystères</h1>

<div class="etape-contenu">
    <p>{{ etape.text }}</p>

    {# Ici, plus tard, on ajoutera l'image si elle existe #}
     {% if etape.image %}
     <img src="/images/{{ etape.image }}" alt="Illustration pour l'étape {{ etape.id }}">
     {% endif %}

    {% if etape.choices %}
        <nav class="choix">
            <ul>
                {% for choix in etape.choices %}
                    <li>
                        <a href="/histoires/ile-mystere/{{ choix.target }}/">{{ choix.text }}</a>
                    </li>
                {% endfor %}
            </ul>
        </nav>
    {% endif %}

    {% if etape.ending %}
        <div class="fin">
            <p><strong>{{ etape.ending.type }} {{ etape.ending.icon }}</strong></p>
            <p>{{ etape.ending.message }}</p>
            {# On garde le lien vers l'accueil, qui pointe vers / (index.md) #}
            <p><a href="/">Retourner à l'accueil</a></p>
        </div>
    {% endif %}

</div>

{# Plus besoin de </body>, </html> ici ! #}